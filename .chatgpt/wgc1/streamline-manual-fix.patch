--- a/source/dlss_bridge.h
+++ b/source/dlss_bridge.h
@@ -20,10 +20,6 @@
 class DlssExperiment {
 public:
     using Log = std::function<void(const std::wstring&)>;
-    using FactoryFn = HRESULT(WINAPI*)(REFIID,void**);
-    using DeviceFn = HRESULT(WINAPI*)(IUnknown*,D3D_FEATURE_LEVEL,REFIID,void**);
-    FactoryFn factory{};
-    DeviceFn createDevice{};
     Log log;
     UINT cw{},ch{},rw{},rh{},ow{},oh{};
     ComPtr<ID3D12Resource> current,previous,motion,depth,result;
@@ -34,6 +30,7 @@
     std::wstring error;
     void Load();
     void BindDevice(ID3D12Device* d) {Check(setDevice(d),"slSetD3DDevice");}
+    void UpgradeInterface(void** p) {Check(upgradeInterface(p),"slUpgradeInterface");}
     void Init(ID3D12Device*,IDXGIAdapter1*,ID3D12Resource*,UINT,UINT,UINT,UINT,
               sl::DLSSMode,sl::DLSSPreset,sl::DLSSGMode,UINT,float,bool,bool);
     void PrepareFrame();
@@ -72,6 +69,7 @@
     PFun_slInit* init{};
     PFun_slShutdown* shutdown{};
     PFun_slSetD3DDevice* setDevice{};
+    PFun_slUpgradeInterface* upgradeInterface{};
     PFun_slGetFeatureFunction* feature{};
     PFun_slIsFeatureSupported* supported{};
     PFun_slGetNewFrameToken* newToken{};
--- a/source/dlss_bridge.cpp
+++ b/source/dlss_bridge.cpp
@@ -119,20 +119,19 @@
         (void)file;
     module=LoadLibraryExW((dir+L"\\sl.interposer.dll").c_str(),nullptr,LOAD_LIBRARY_SEARCH_DLL_LOAD_DIR|LOAD_LIBRARY_SEARCH_DEFAULT_DIRS);
     if(!module) throw std::runtime_error("Cannot load sl.interposer.dll; extract the entire ZIP first.");
-    Export(init,"slInit"); Export(shutdown,"slShutdown"); Export(setDevice,"slSetD3DDevice");
+    Export(init,"slInit"); Export(shutdown,"slShutdown"); Export(setDevice,"slSetD3DDevice"); Export(upgradeInterface,"slUpgradeInterface");
     Export(feature,"slGetFeatureFunction");Export(supported,"slIsFeatureSupported");
     Export(newToken,"slGetNewFrameToken");Export(constants,"slSetConstants");Export(tags,"slSetTagForFrame");
     Export(evaluate,"slEvaluateFeature");Export(freeResources,"slFreeResources");
-    Export(factory,"CreateDXGIFactory1"); Export(createDevice,"D3D12CreateDevice");
     const wchar_t* dirs[]={dir.c_str()};
     sl::Feature features[]={sl::kFeatureDLSS,sl::kFeatureDLSS_G,sl::kFeatureReflex,sl::kFeaturePCL};
     sl::Preferences p{};p.pathsToPlugins=dirs;p.numPathsToPlugins=1;p.pathToLogsAndData=dir.c_str();
     p.logLevel=sl::LogLevel::eDefault;p.featuresToLoad=features;p.numFeaturesToLoad=4;
-    p.flags=sl::PreferenceFlags::eDisableCLStateTracking|sl::PreferenceFlags::eUseFrameBasedResourceTagging|sl::PreferenceFlags::eUseDXGIFactoryProxy;
+    p.flags=sl::PreferenceFlags::eDisableCLStateTracking|sl::PreferenceFlags::eUseFrameBasedResourceTagging|sl::PreferenceFlags::eUseManualHooking;
     p.engine=sl::EngineType::eCustom;p.engineVersion="WGC1-DX12 Low Latency DLSS SR + MFG 2x-6x Dynamic";
     p.projectId="e86d5d1d-b886-47ac-a90d-48708688358b";p.renderAPI=sl::RenderAPI::eD3D12;
     Check(init(p,sl::kSDKVersion),"slInit");initialized=true;
-    log(L"Streamline 2.14.1 initialized: DLSS SR all modes + DLSS Frame Generation fixed/auto/dynamic.");
+    log(L"Streamline 2.14.1 initialized in manual-hooking mode: native D3D12 device + upgraded queue/factory/swapchain hooks.");
 }
 void DlssExperiment::Texture(ComPtr<ID3D12Resource>& r,UINT w,UINT h,DXGI_FORMAT f,D3D12_RESOURCE_STATES state) {
     D3D12_HEAP_PROPERTIES hp{};hp.Type=D3D12_HEAP_TYPE_DEFAULT;
--- a/source/main.cpp
+++ b/source/main.cpp
@@ -294,7 +294,7 @@
 }
 
 bool App::PickAdapter(ComPtr<IDXGIFactory6>& f,ComPtr<IDXGIAdapter1>& a){
-    HRESULT hr=gDLSS.factory(IID_PPV_ARGS(&f));if(FAILED(hr)){PostStatus(L"DXGI factory: "+HrText(hr));return false;}
+    HRESULT hr=CreateDXGIFactory2(0,IID_PPV_ARGS(&f));if(FAILED(hr)){PostStatus(L"DXGI factory: "+HrText(hr));return false;}
     for(UINT i=0;;++i){ComPtr<IDXGIAdapter1> x;hr=f->EnumAdapterByGpuPreference(i,DXGI_GPU_PREFERENCE_HIGH_PERFORMANCE,IID_PPV_ARGS(&x));if(hr==DXGI_ERROR_NOT_FOUND)break;if(FAILED(hr))continue;DXGI_ADAPTER_DESC1 ad{};x->GetDesc1(&ad);if(ad.Flags&DXGI_ADAPTER_FLAG_SOFTWARE)continue;ComPtr<ID3D12Device> test;if(SUCCEEDED(D3D12CreateDevice(x.Get(),D3D_FEATURE_LEVEL_11_0,IID_PPV_ARGS(&test)))){a=x;gLog.Write(std::wstring(L"Selected adapter: ")+ad.Description);return true;}}
     return false;
 }
@@ -322,9 +322,23 @@
 
 bool App::InitDx12(HWND output,WgcCapture& cap,IDXGIAdapter1* adapter,const Settings&s,Dx12State& d){
     if(!adapter)return false;
-    HR(CreateDXGIFactory2(0,IID_PPV_ARGS(&d.factory)),"DXGI factory");
     d.adapter=adapter;
-    HR(gDLSS.createDevice(d.adapter.Get(),D3D_FEATURE_LEVEL_11_0,IID_PPV_ARGS(&d.device)),"Streamline D3D12 device");gDLSS.BindDevice(d.device.Get());
+
+    // Streamline manual-hooking mode: create a native device, bind it immediately,
+    // then upgrade only the interfaces whose methods DLSS-G must intercept.
+    ComPtr<ID3D12Device> nativeDevice;
+    HR(D3D12CreateDevice(d.adapter.Get(),D3D_FEATURE_LEVEL_11_0,IID_PPV_ARGS(&nativeDevice)),"Native D3D12 device");
+    gDLSS.BindDevice(nativeDevice.Get());
+    ID3D12Device* proxyDevice=nativeDevice.Get();
+    gDLSS.UpgradeInterface(reinterpret_cast<void**>(&proxyDevice));
+    d.device=proxyDevice;
+
+    ComPtr<IDXGIFactory6> nativeFactory;
+    HR(CreateDXGIFactory2(0,IID_PPV_ARGS(&nativeFactory)),"Native DXGI factory");
+    IDXGIFactory6* proxyFactory=nativeFactory.Get();
+    gDLSS.UpgradeInterface(reinterpret_cast<void**>(&proxyFactory));
+    d.factory=proxyFactory;
+
     D3D12_COMMAND_QUEUE_DESC q{};q.Type=D3D12_COMMAND_LIST_TYPE_DIRECT;q.Priority=D3D12_COMMAND_QUEUE_PRIORITY_NORMAL;HR(d.device->CreateCommandQueue(&q,IID_PPV_ARGS(&d.queue)),"DX12 queue");
     D3D12_DESCRIPTOR_HEAP_DESC rh{};rh.Type=D3D12_DESCRIPTOR_HEAP_TYPE_RTV;rh.NumDescriptors=kBackBufferCount;HR(d.device->CreateDescriptorHeap(&rh,IID_PPV_ARGS(&d.rtvHeap)),"RTV heap");d.rtvStride=d.device->GetDescriptorHandleIncrementSize(D3D12_DESCRIPTOR_HEAP_TYPE_RTV);
     D3D12_DESCRIPTOR_HEAP_DESC sh{};sh.Type=D3D12_DESCRIPTOR_HEAP_TYPE_CBV_SRV_UAV;sh.NumDescriptors=1;sh.Flags=D3D12_DESCRIPTOR_HEAP_FLAG_SHADER_VISIBLE;HR(d.device->CreateDescriptorHeap(&sh,IID_PPV_ARGS(&d.srvHeap)),"SRV heap");
